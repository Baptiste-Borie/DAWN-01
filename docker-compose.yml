services:
  nginx:
    image: nginx:alpine
    container_name: dawn-01
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

